# Default Folder Structure to make a Full-Stack React App with an express + sqlite backend

## api
 - seeding commands
  Use the sequelize cli to generate a User model `npx sequelize model:create --name User --attributes firstName:string,lastName:string,emailAddress:string,password:string`
  Use the sequelize cli to generate a User model `npx sequelize model:create --name Course --attributes title:string,description:text,estimatedTime:string,materialsNeeded:string`